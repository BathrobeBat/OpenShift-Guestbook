namespace: grupp4

images:
  backend: "quay.io/bathrobebat/guestbook:backend"
  frontend: "quay.io/bathrobebat/guestbook:frontend-v2"
  postgres: "quay.io/fedora/postgresql-16:latest"
  redis: "quay.io/kurs/redis:latest"

imagePullPolicy: IfNotPresent

replicas:
  backend: 2
  frontend: 2

storage:
  postgresSize: 1Gi
  redisSize: 1Gi

db:
  name: guestbook

secrets:
  dbUser: guestbook
  dbPassword: superhemligt-losen        # ändra sen
  redisPassword: annu-mer-hemligt       # ändra sen

route:
  enabled: true
  host: ""   # låt OpenShift generera host
  tls:
    enabled: true
    termination: edge
